# Ambient audio assets

Empty by design. See CREDITS.md for the sourcing rules (CC0 only) and the
expected filenames. The app runs without these; the picker simply cannot start
a sound until the matching file exists.
